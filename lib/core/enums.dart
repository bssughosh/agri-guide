enum LoginStatus {
  LOGGED_IN,
  LOGGED_OUT,
}

enum SelectionListType {
  STATE,
  DISTRICT,
}

enum DownloadParams {
  temp,
  humidity,
  rainfall,
}

enum TableType {
  TEMPERATURE,
  HUMIDITY,
  RAINFALL,
}

enum StatisticsFilters {
  Temperature,
  Humidity,
  Rainfall,
}
